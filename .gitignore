.build/
.swiftpm/
DerivedData/
*.xcuserdata/
.cocogram.local

# Packaging output and signing secrets — never commit these.
dist/
*.p8
*.icns
# ...but DO track the committed fallback app icon, so packaging can always bundle an icon
# even if on-the-fly generation (scripts/make_icon.swift) fails. See package.sh step 4.
!Resources/AppIcon.icns
